version: "3.8"

services:
  frontend:
    build:
      context: ./anecdotes
      dockerfile: dev.Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./anecdotes:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true